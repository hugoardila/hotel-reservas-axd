services:

  hotel-web:
    build: .
    container_name: hotel-reservas-web

    restart: unless-stopped

    ports:
      - "127.0.0.1:18160:8000"

    volumes:
      - ./data:/data

    environment:
      HOTEL_DB_PATH: /data/hotel.sqlite
